#include <tunables/global>

profile unbound flags=(attach_disconnected,mediate_deleted) {
  #include <abstractions/base>

  file,
  signal (send) set=(kill,term,int,hup,cont),
  signal (receive) set=(kill,term,int,hup,cont,usr1,usr2),

  capability chown,

  # S6-Overlay & Bashio
  /bin/** ix,
  /etc/services.d/** rwix,
  /init rix,
  /run/** rw,
  /run/s6/** rwkix,
  /tmp/** rw,
  /usr/bin/** ix,
  /usr/lib/bashio/** ix,

  # Access to options and config
  /config/** rw,
  /data/** rw,

  # Transition to child profile for the daemon
  /usr/sbin/unbound cx,

  profile /usr/sbin/unbound flags=(attach_disconnected,mediate_deleted) {
    #include <abstractions/base>
    #include <abstractions/nameservice>

    signal (receive) set=(kill,term,int,hup,cont,usr1,usr2),

    capability net_bind_service,
    capability setgid,
    capability setuid,

    network inet dgram,
    network inet stream,
    network inet6 dgram,
    network inet6 stream,

    /config/** rw,
    /dev/null rw,
    /dev/urandom r,
    /etc/unbound/** rw,
    /proc/** r,
    /usr/sbin/unbound rm,
  }
}
